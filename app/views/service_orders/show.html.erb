<% content_for :page_title do %>
<%= render 'title' %>
<% end %>

<div class="row">
  <div class="col-md-12">
    <div class="portlet box">
      <div class="portlet-body form">
        <!-- BEGIN FORM-->
        <%= form_for(@service_order, html: { class: "form-horizontal" }) do |f| %>
          <div class="form-body">
            <%= f.error_messages %>
            <h4 class="form-section">基本信息</h4>
            <div class="form-group">
              <%= render "shared/column", name: f.c_name_field(:no), content: f.c_text_field(:no) %>
              <%= render "shared/column", name: f.c_name_field(:appointed_time), content: f.c_text_field(:appointed_time) %>
            </div>
          </div>
          <div class="form-actions right">
            <button type="submit" class="btn">保存</button>
          </div>
        <% end %>
        <!-- END FORM-->
      </div>
    </div>
  </div>
</div>